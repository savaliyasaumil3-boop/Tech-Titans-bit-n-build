fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.1.0
supabase>=2.3.0
python-dotenv>=1.0.0
scikit-learn>=1.4.0
ortools>=9.8.3296
pandas>=2.2.0
numpy>=1.26.0
requests>=2.31.0
python-multipart>=0.0.9
pillow>=10.2.0

# ── AI Model Dependencies ───────────────────────────────────────────────────
# PyTorch + torchvision for real MobileNetV2 waste classification inference.
# CPU-only wheels (~250 MB) are sufficient — no GPU required.
# Install with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Or for GPU (CUDA 12.1):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0
